name: deploy-book

# 🔄 Ejecuta este workflow en cada push a main o master
on:
  push:
    branches:
      - main
      - master

jobs:
  deploy-book:
    runs-on: ubuntu-latest

    # 🔑 Permisos requeridos para publicar en GitHub Pages
    permissions:
      contents: read         # leer repo
      pages: write           # publicar en Pages
      id-token: write        # OIDC auth

    environment:
      name: github-pages     # ambiente de deploy
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      # ✅ Clona el repositorio
      - name: Checkout repository
        uses: actions/checkout@v4

      # ✅ Configura Python 3.11 (ajusta aquí si cambias versión de tu Jupyter Book)
      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: 3.11

      # ✅ Instala dependencias de tu proyecto
      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      # ✅ Cachea notebooks ejecutados (opcional, acelera builds futuros)
      - name: Cache executed notebooks
        uses: actions/cache@v4
        with:
          path: _build/.jupyter_cache
          key: jupyter-book-cache-${{ hashFiles('requirements.txt') }}

      # ✅ Construye el Jupyter Book
      - name: Build the book
        run: |
          jupyter-book build .

      # ✅ Empaqueta y sube los archivos para GitHub Pages
      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v2  # v2 es la última estable y usa internamente upload-artifact@v4
        with:
          path: "_build/html"

      # ✅ Despliega a GitHub Pages
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2  # usa v2 estable o v4 si ya ha sido liberada oficialmente
